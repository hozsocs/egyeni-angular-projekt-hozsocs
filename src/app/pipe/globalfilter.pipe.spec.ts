import { GlobalfilterPipe } from './globalfilter.pipe';

describe('GlobalfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GlobalfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
